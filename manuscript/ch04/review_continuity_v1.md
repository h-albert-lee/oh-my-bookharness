# Continuity Review: ch04

## Score

- continuity: **4/5**
- dependency_coverage: **3/5**

## Must Fix

- 3장 참조 오류: 도입부에서 '3장에서 살펴봤듯, 실패의 원인은 모델 자체가 아닌 시스템에 있는 경우가 많다'고 언급하나, chapter_dependencies에 ch03 요약이 제공되지 않아 해당 참조가 실제 3장 내용과 일치하는지 검증 불가. 현재 제공된 이전 장 요약은 ch01뿐이므로, 3장 참조를 도입부에서 이렇게 확정적으로 사용하는 것은 연속성 리스크가 있음.
- 프롬프트 구성기 섹션이 의사코드 중간('# 비즈니스' 주석)에서 잘려 미완성 상태임. 섹션이 완결되지 않아 이후 구성 요소(LLM 호출기, 출력 처리기, 상태 관리자, 오류 처리기)가 전혀 다루어지지 않음. 그림 4-1에서 6개 컴포넌트를 예고했으나 실제로 2개(입력 처리기, 프롬프트 구성기 일부)만 서술됨.
- 그림 4-1 참조 문제: 본문에서 '그림 4-1은 이 장에서 다룰 LLM 파이프라인의 전체 흐름을 보여준다'고 언급하나, 실제 그림 캡션이나 그림 자체가 초안에 존재하지 않음. 독자가 참조할 수 없는 그림을 본문에서 확정적으로 언급하는 것은 수정 필수.

## Should Fix

- ch01 용어 일관성: ch01 요약에서 'harness'는 '하네스'로 한글 표기하고 있으나, 4장 초안에서도 동일하게 '하네스(Harness)'로 병기 후 사용하는 것은 일관적임. 다만 ch01에서 정의된 'nondeterminism(비결정성)' 개념이 4장 도입에서 전혀 언급되지 않음. 파이프라인 구조의 필요성을 논증할 때 '비결정성 때문에 각 단계의 경계가 더욱 중요하다'는 연결 고리가 있으면 ch01→ch04 논리적 흐름이 강화됨.
- ch01 그림 1-2 재참조 방식 개선: '파이프라인 관점' 섹션 마지막 문단에서 '1장의 그림 1-2에서 소개한 하네스는 이 파이프라인 전체를 외부에서 감싸는 구조'라고 언급하는데, 도입부에서 이미 그림 1-2를 먼저 언급함. 두 번의 참조 중 도입부에서의 참조('그림 1-2는 모델 단독 실행과, 하네스가 모델을 감싸는 실행 구조를 대비해서 보여준다')가 더 충실하고, 후반부 참조는 '앞서 언급한 그림 1-2' 식으로 중복임을 명시하거나 통합하는 것이 좋음.
- ch01의 '입력 통제, 출력 평가, 실패 처리' 세 축이 핵심 개념으로 정의되어 있으나, 4장에서 소개하는 6개 컴포넌트(입력 처리기, 프롬프트 구성기, LLM 호출기, 출력 처리기, 상태 관리자, 오류 처리기)와의 매핑이 명시적으로 제시되지 않음. '출력 평가'→출력 처리기, '실패 처리'→오류 처리기 식의 연결을 한 문장이라도 언급하면 ch01 의존 개념 커버리지가 높아짐.
- 표 1-2(실패 귀인 오류 유형)가 ch01의 핵심 개념으로 명시되어 있으나, 4장에서 '실패가 LLM 호출 단계에서 발생하지만 원인은 입력 단계'라는 논의는 실패 귀인 오류와 직결됨에도 표 1-2를 명시적으로 참조하지 않음. '1장의 실패 귀인 오류 유형(표 1-2)에서 본 것처럼' 식의 참조를 추가하면 연속성이 강화됨.

## Nice to Have

- 도입부의 챗봇 실패 시나리오(프롬프트 수정→모델 교체→막다른 벽)가 ch01의 '데모 환경과 운영 환경의 차이' 서사와 자연스럽게 이어지지만, 독자에게 이 연결을 명시적으로 짚어주는 한 문장('1장에서 다룬 데모-운영 격차가 실제로 어떤 형태로 나타나는지를 보여주는 사례다')을 추가하면 독자 경험이 개선됨.
- 프롬프트 인젝션(Prompt Injection) 용어가 입력 처리기 섹션에서 처음 등장하는데, 이 용어가 이 책에서 첫 번째 등장인지 확인 필요. 첫 등장이라면 tone_guide 지침에 따라 '프롬프트 인젝션(Prompt Injection)'으로 병기한 현재 표기는 적절하나, 이후 동일 장 내에서는 '프롬프트 인젝션'으로만 사용하는 일관성 유지 필요.
- 파이프라인 관점 섹션에서 MVC 아키텍처 비유와 미들웨어 체인 비유 두 가지를 모두 사용하는데, 독자층이 백엔드 개발자 중심임을 감안하면 두 비유 모두 적절하나 흐름상 하나를 주 비유로, 다른 하나를 보조로 명확히 위계를 두면 가독성이 개선됨.

## Binary Checks

- dependency_concepts_covered: ✗ Fail
- has_prior_chapter_reference: ✓ Pass
- has_forward_reference: ✓ Pass
